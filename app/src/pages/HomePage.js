import React from 'react';
import { Typography } from '@material-ui/core';

const HomePage = () => {
    return <Typography variant='h5' align='center' >
        Bem vindo(a) ao app de monitoramento de temperatura!
    </Typography>
}

export default HomePage;